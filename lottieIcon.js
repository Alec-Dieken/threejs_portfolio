import lottie from 'lottie-web';

// const container = document.getElementById('lottie-container');

// const animation = lottie.loadAnimation({
//   container: container, // the DOM element that will contain the animation
//   renderer: 'svg', // the rendering mode: 'svg', 'canvas', or 'html'
//   loop: true, // whether the animation should loop
//   autoplay: true, // whether the animation should autoplay
//   path: '/public/assets/lotties/wheelScroll.json', // the path to the JSON file
// });

// animation.play();

// To control the animation programmatically:
// animation.play();
// animation.stop();
// animation.pause();
// animation.setSpeed(0.5);
